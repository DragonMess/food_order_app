import React from "react";
import classes from "./MealItemForm.modules.css";

const MealItemForm = (props) => {
  return (
    <form className={classes.meal}>
      <input />
      <button>+ Add</button>
    </form>
  );
};
export default MealItemForm;
